/* ========================================
   Busy Beaver — minimal interactions

   Navigation smooth-scroll and section offsets are handled in CSS
   (html { scroll-behavior: smooth } + scroll-margin-top on sections),
   so no JavaScript is required for in-page anchors.
   ======================================== */
